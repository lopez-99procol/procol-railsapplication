<% if @user.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@user.errors.count,"error")%> prohibited this for beeing saved</h2>
    <p>There were problems with the following fields</p>
    <u1>
      <% @users.errors.full_messages.each do |msg| %>
      <li><%= msg%>
      <% end %>
    </ul>
  </div>
<% end %>